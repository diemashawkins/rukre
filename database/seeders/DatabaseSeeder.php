<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * RUKRE deliberately seeds no accounts, so no default password ever exists.
     * Create users with `php artisan rukre:user` and fill the library with
     * `php artisan rukre:scan`.
     */
    public function run(): void
    {
        //
    }
}
